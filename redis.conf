# Minimal Redis configuration for Windows

# Bind to localhost only
bind 127.0.0.1

# Listen on TCP port 6379
port 6379

# (Optional) You can add other settings below as needed.
# Make sure any unixsocket or related directives are commented out.
# unixsocket /tmp/redis.sock
